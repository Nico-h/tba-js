import React from 'react';
import MButton from '@material-ui/core/Button';

const Button =() => {
    return (
        <MButton variant="contained" color="primary">VALIDER</MButton>
    )
};

export default Button;
